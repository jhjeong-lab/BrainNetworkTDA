# BrainNetworkTDA
pip install numpy
pip install pandas
pip install matplotlib
pip install seaborn
pip install nilearn
pip install scikit-learn # nilearn이 scikit-learn에 의존할 수 있습니다.
pip install scipy # ripser나 다른 과학 계산 패키지가 의존할 수 있습니다.
pip install ripser
pip install plotly
